'use client';
import {useState} from 'react';
import {Button,Card,Input} from '@/components/ui';
export default function Login(){const [error,setError]=useState('');const [busy,setBusy]=useState(false);return <div className="mx-auto mt-20 max-w-md"><div className="mb-8 text-center"><span className="text-cyan-400 text-sm tracking-widest">NEXACRED</span><h1 className="text-3xl font-semibold mt-3">Seu próximo contato,<br/>com mais controle.</h1><p className="mt-3 text-slate-400">Entre no painel de relacionamento.</p></div><Card className="p-7"><form className="space-y-5" onSubmit={async e=>{e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget);try{const res=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(f))});if(!res.ok)throw new Error('E-mail ou senha inválidos');window.location.href='/';}catch(e){setError((e as Error).message);}finally{setBusy(false);}}}><label className="block text-sm">E-mail<Input name="email" type="email" autoComplete="username" required className="mt-2"/></label><label className="block text-sm">Senha<Input name="password" type="password" autoComplete="current-password" required className="mt-2"/></label>{error&&<p role="alert" className="text-red-400">{error}</p>}<Button disabled={busy} className="w-full">{busy?'Entrando…':'Entrar no painel'}</Button></form></Card></div>;}
